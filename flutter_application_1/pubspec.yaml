name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  universal_html: ^2.2.4
  # video_player_web: ^2.3.1
  # video_player: ^2.9.1
  pos_printer_manager: ^0.0.5
  firebase_core: ^3.2.0
  firebase_crashlytics: ^4.3.0
  firebase_auth: ^5.4.0
  # firebase_remote_config: ^4.1.4
  cloud_firestore: ^5.6.1
  firebase_database: ^11.3.0
  equatable: ^2.0.7
  flutter_bloc: ^8.1.6
  dartz: ^0.10.1
  either_dart: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/video/
    - assets/images/
